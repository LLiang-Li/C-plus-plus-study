#include "Header.h"

using namespace std;
static bool odd(int n) { return n % 2; }

int main1()
{

    // std::vector<int> d (50000000);

    // std::mt19937 gen;
    // std::uniform_int_distribution<int> dis(0, 100000);
    // auto rand_num ([=] () mutable { return dis(gen); });

    // generate(std::begin(d), std::end(d), rand_num);

    return 0;
}